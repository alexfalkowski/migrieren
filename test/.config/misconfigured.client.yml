client:
  v1:
    host: localhost:8080
    timeout: 1s
    database: invalid_source
    version: 1
otel:
  kind: jaeger
  host: localhost:6831
transport:
  port: 8080
  grpc:
    user_agent: "Migrieren-client/1.0 grpc/1.0"
    retry:
      timeout: 2s
      attempts: 3
