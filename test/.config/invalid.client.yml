client:
  v1:
    host: localhost:9090
    timeout: 1s
    database: postgres
    version: 1
telemetry:
  tracer:
    host: localhost:4318
    secure: false
transport:
  port: 8080
  grpc:
    user_agent: "Migrieren-client/1.0 grpc/1.0"
    retry:
      timeout: 2s
      attempts: 3
